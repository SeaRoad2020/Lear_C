/*分队列，一列人喊1,2.喊1的人为第一列，要求输出最后一个字符没有空格*/
#include <stdio.h>
  int main()
  {
  	int n=0;
  	scanf("%d",&n);
  	if (n%2==0){
  		int i=0;
  		for(i=1;i<n;i+=2){
  			printf(" %d",i);
		  }
  		
	  }
	  else{
	  	int i=0;
	  		for(i=1;i<=n;i+=2){
  			printf(" %d",i);
		  }
	  }
  }
